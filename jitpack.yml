# before_install:
#  - bash ./install.sh
install: 
# - mvn install -Dfile=$POM_PATH
 - bash ./install.sh
env: 
#  POM_PATH: "./pom.xml"
  VERSION: "v4.13.0"
  GROUP: "org.eclipse.birt.runtime"
  ARTIFACT: "birt-libs"
  
  
